{
  "$schema": "https://turbo.build/schema.json",
  "globalDependencies": ["./.env.*local"],
  "globalEnv": ["NODE_ENV"],
  "tasks": {
    "build": {
      "inputs": [".env.local", ".env.production", ".env.development", ".env"],
      "dependsOn": ["^build"],
      "outputs": ["dist/**"]
    },
    "pre:deploy": {
      "dependsOn": ["build"],
      "inputs": [".env.local", ".env.production", ".env", ".env.development", "dist/**"],
      "outputs": [".open-next/**"]
    },
    "deploy": {
      "dependsOn": ["^pre:deploy"],
      "cache": false,
      "persistent": true
    },
    "lint": {},
    "validate": {},
    "typecheck": {},
    "validate:check": {},
    "test": {},
    "test:coverage": {},
    "dev": {
      "cache": false,
      "persistent": true
    }
  }
}
